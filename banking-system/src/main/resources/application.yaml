spring:
  datasource:
    username: postgres
    password: gc
    url: jdbc:postgresql://localhost:5432/Banking

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080
